<!--{assign var="commentStart" value="<!--"}-->
<!--{assign var="commentEnd" value="-->"}-->

<!--{* The regex on $content prevents XSS via HTML comment breakout attack: the regex replaces any occurrence of '-' that is
  immediately followed by another '-' with '- ' (dash + space), preventing comment breakout via HTML comment closing tag *}-->

<!--{$commentStart}--><!--{$content|regex_replace:'/-(?=-)/':'- '|default:''}--><!--{$commentEnd}-->
<noinclude>
[[Category:MultiWiki]]
</noinclude>