<pre>
NetworkManager[672]:   [1726561197.0596] failed to open /run/network/ifstate

pinctrl core: failed to create debugfs directory

zswap: debugfs initialization failed

Calamares supports LUKS2, but you have to explicitly set it (which we do) and either have LUKS2 support in GRUB (which we don't) or have unencrypted /boot (which we do).
</pre>