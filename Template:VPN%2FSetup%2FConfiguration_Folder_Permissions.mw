Since OpenVPN will be run under user <i>tunnel</i>, that user requires read access to the folder <i>/etc/openvpn</i>.

{{CodeSelect|code=
sudo chown -R tunnel:tunnel /etc/openvpn
}}

{{CodeSelect|code=
sudo chown -R tunnel:tunnel /run/openvpn
}}